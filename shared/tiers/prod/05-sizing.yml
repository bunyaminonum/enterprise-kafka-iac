---
kafka_controller_service_environment_overrides:
  KAFKA_HEAP_OPTS: "-Xms4g -Xmx4g"

kafka_broker_service_environment_overrides:
  KAFKA_HEAP_OPTS: "-Xms16g -Xmx16g -XX:MetaspaceSize=256m -XX:+UseG1GC"

schema_registry_service_environment_overrides:
  SCHEMA_REGISTRY_HEAP_OPTS: "-Xms4g -Xmx4g"

kafka_connect_service_environment_overrides:
  KAFKA_HEAP_OPTS: "-Xms8g -Xmx8g"

kafka_rest_service_environment_overrides:
  KAFKAREST_HEAP_OPTS: "-Xms4g -Xmx4g"

control_center_next_gen_service_environment_overrides:
  CONTROL_CENTER_HEAP_OPTS: "-Xmx6g"
